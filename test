node {
  try {
    stage('checkout') {
      checkout scm
    }
    stage('test') {
      sh "#!/bin/bash echo shell commands here"
    }
  }
  finally {
    stage('cleanup') {
      echo "doing some cleanup..."
    }
  }
}
