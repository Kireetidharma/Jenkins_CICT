node {
  try {
    stage('checkout') {
      checkout scm
    }
    stage('executable mode'){
    chmod +x hello.sh
    }
    stage('test') {
      bash hello.sh
    }
  }
  finally {
    stage('cleanup') {
      echo "doing some cleanup..."
    }
  }
}
